# Server Configuration
PORT=4000
NODE_ENV=production

# CORS Configuration
# Use "*" for all origins (not recommended for production)
# Use comma-separated values for multiple origins: https://app1.com,https://app2.com
CORS_ORIGIN=https://yourdomain.com
